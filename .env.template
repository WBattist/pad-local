# Copying this file is not required. install.ps1 and pad generate runtime.env automatically.
# Non-secret defaults live in config/defaults.env.
# Generated secrets live outside source control in config/runtime.env.

APP_PORT=8000
CODER_PORT=7080
KEYCLOAK_PORT=8080
